' NOTE: You can use the "Rename" command on the context menu to change the class name "tes" in code, svc and config file together.
Public Class tes
    Implements Ites

    Public Sub DoWork() Implements Ites.DoWork
    End Sub

End Class
